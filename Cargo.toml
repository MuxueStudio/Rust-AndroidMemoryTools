[package]
name = "AndroidMemoryTools"
version = "0.1.0"
edition = "2021"
[dependencies]
libc = "0.2"
[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["handleapi"] }